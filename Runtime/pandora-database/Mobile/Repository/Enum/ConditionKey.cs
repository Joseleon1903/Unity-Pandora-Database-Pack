namespace Unity.Pandora.Database.Mobile.Repository.Enum
{
    public enum ConditionKey
    {
        WHERE,
        OREDER_BY,
        GROUP_BY,
        DISTINCT,
        HAVING,
        LIMIT
    }
}